
#include <stdio.h>
#include <math.h>

double number, m,n,c,a;
double sumseries;

double sum, f = 1, i;

int main()
{
    printf("\n Enter the value:  ");
    scanf("%lf", &number);
    number = tgamma(n+1);
    

       for (int i = 0; i <= n; i++)
    {
        sum += (i / i);

    }

    c = number / sum;

    printf("\n Sum of the above series = %0.f \n", c);
   
    return (0);
}

